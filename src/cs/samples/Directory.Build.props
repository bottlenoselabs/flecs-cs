<Project>

    <Import Project="$(MSBuildThisFileDirectory)/../Directory.Build.props" />

    <!-- MSBuild settings -->
    <PropertyGroup>
        <IsPackable>false</IsPackable>
    </PropertyGroup>

</Project>